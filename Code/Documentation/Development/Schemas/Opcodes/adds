Name:
  adds
Description:
  Pop last two values from stack and add them together. Result is in stack
Argument:
  0 : LHS
  1 : RHS
Code:
  PtSigned32 l, r;
  r = Vm_Pop();
  l = Vm_Pop();
  Vm_Push(l + r);
